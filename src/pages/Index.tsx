import { RadioOverlay } from '@/components/overlay/RadioOverlay';

const Index = () => {
  return (
    <div className="flex items-center justify-center min-h-screen bg-background overflow-auto">
      <RadioOverlay />
    </div>
  );
};

export default Index;
